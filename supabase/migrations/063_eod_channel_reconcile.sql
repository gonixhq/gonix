-- ════════════════════════════════════════════════════════════
-- 063: ปิดยอด — เก็บยอดนับจริงของโอน/บัตร (reconcile ทุกช่องทาง)
-- ════════════════════════════════════════════════════════════
-- เดิมมีแค่ actual_cash (เงินสด) — เพิ่มให้โอน(เทียบ K Shop)/บัตร(เทียบ EDC)
-- ตรวจนับจริงด้วยมาตรฐานเดียวกับเงินสด
-- ════════════════════════════════════════════════════════════

ALTER TABLE clinic_day_closes
    ADD COLUMN IF NOT EXISTS transfer_actual numeric(12,2),   -- ยอดโอนนับจริง (จาก K Shop)
    ADD COLUMN IF NOT EXISTS credit_actual   numeric(12,2);   -- ยอดบัตรนับจริง (จาก EDC)
