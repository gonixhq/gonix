-- ════════════════════════════════════════════════════════════
-- 064: invoice_headers.vn ให้เป็น nullable — รองรับใบเสร็จขายคอสตรง (ไม่ผูก visit)
-- ════════════════════════════════════════════════════════════
-- เดิม vn NOT NULL → ขายคอสจากเวชระเบียนออกใบเสร็จไม่ได้ (ไม่มี visit)
-- ทำให้ vn null ได้ → ขายคอสสร้างใบเสร็จ + payment เข้าระบบการเงินได้
-- ════════════════════════════════════════════════════════════

ALTER TABLE invoice_headers ALTER COLUMN vn DROP NOT NULL;
